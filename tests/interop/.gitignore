# Everything run.sh writes: the trust anchors setup() derives, the normalised
# zone dumps a failed comparison is diffed from, the per-peer transfer logs.
# Regenerated by every run and meaningless without the containers they came
# from.
run/
