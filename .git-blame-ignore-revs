# Commits that changed formatting only, and should not show up in `git blame`.
#
# Enable it once per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a revision here if it is *purely* mechanical — see CLAUDE.md §12. A
# commit listed here is one whose authorship git will hide, so anything with a
# real change in it must not be on this list.

# cargo fmt --all: the first run, 36 files. The repo predates the formatter.
#
# This hash changed on 2026-08-01, when the session-URL trailers were stripped
# from every commit message, and again on 2026-08-30, when the history was
# squashed into a 54-patch series. A stale entry here is not cosmetic: `git
# blame` fails outright on a revision it cannot resolve.
dc5b9ca8be5ffd52d16f32f86b0304ac233ca739
