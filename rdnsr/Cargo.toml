[package]
name = "rdnsr"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Recursive DNS resolver with caching, and a forwarding mode. Separate from the authoritative server (rdnsd)."
license.workspace = true

[dependencies]
clap.workspace = true
rdns.workspace = true
rdns-transport.workspace = true
tokio.workspace = true
anyhow.workspace = true
tracing.workspace = true
# No `tracing-subscriber`: `rdns::logging::init` installs the subscriber and
# this binary never names the crate (`TODO.md` #30n).
